# Railway Nixpacks Configuration
# Ensures PostgreSQL client libraries are available

[phases.setup]
nixPkgs = ["postgresql"]

[phases.install]
cmds = ["pip install --upgrade pip"]

[start]
cmd = "uvicorn src.main:app --host 0.0.0.0 --port $PORT"
